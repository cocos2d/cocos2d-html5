var ChipmunkTest = cc.Layer.extend({
    init : function(){
        this._super();
        var label = cc.LabelTTF.create("See test case of ccchipmunk in ccphysics module please!")
        var winSize = cc.Director.getInstance().getWinSize();
        this.addChild(label);
        label.setPosition(winSize.width/2, winSize.height/2);
        return true;
    }
});
ChipmunkTest.create = function(args){
    var layer = new ChipmunkTest();
    layer.init();
    return layer;
};