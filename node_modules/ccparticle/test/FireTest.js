/**
 * Created by Huabin LING on 12/31/13.
 */

var FireTest = ParticleTest.extend({
    onEnter:function () {
        this._super();

        // Create particle emitter
        this._emitter = cc.ParticleFire.create();
        this._background.addChild(this._emitter, 10);
        // Set particle texture
        this._emitter.setTexture(cc.TextureCache.getInstance().addImage(res.fire_png));

        // Set emission relative to parent
        this._emitter.setPositionType(cc.PARTICLE_TYPE_RELATIVE);
        // Set particle shapeType (only on canvas render mode)
        if (this._emitter.setShapeType)
            this._emitter.setShapeType(cc.PARTICLE_BALL_SHAPE);

        this.setEmitterPosition();
        this.scheduleUpdate();
    }
});

FireTest.create = function() {
    var layer = new FireTest();
    return layer;
}